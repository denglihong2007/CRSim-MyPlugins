<Page x:Class="Guangzhou.PlatformScreen.Views.ScreenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:pp="https://www.cnblogs.com/pumbaa"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:converters="clr-namespace:CRSim.ScreenSimulator.Converters;assembly=CRSim.ScreenSimulator"
        FontFamily="宋体" FontSize="36" Foreground="#FECC00" Background="Black"
        xmlns:views="clr-namespace:CRSim.ScreenSimulator.Views;assembly=CRSim.ScreenSimulator">
    <Page.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold" />
        </Style>
        <converters:DateTimeToStringConverter x:Key="DateTimeToStringConverter" Format="HH:mm" />
        <converters:DateTimeToStringConverter x:Key="DateTimeToDayConverter" Format="dd" />
        <converters:DateTimeToStringConverter x:Key="DateTimeToMonthConverter" Format="MM" />
    </Page.Resources>
    <StackPanel Orientation="Horizontal">
        <StackPanel Orientation="Vertical" Background="#0F0F18">
            <StackPanel.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="FontWeight" Value="Normal" />
                    <Setter Property="FontFamily" Value="黑体"/>
                    <Setter Property="Foreground" Value="White"/>
                </Style>
            </StackPanel.Resources>
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding ThisPlatform}" Grid.RowSpan="2" FontSize="80" Margin="0,0,8,0"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="站台" FontSize="36" VerticalAlignment="Bottom"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="Platform" FontSize="17"/>
                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding CurrentTime,Converter={StaticResource DateTimeToMonthConverter}}" FontSize="28" Foreground="#FECC00" Background="Black" FontFamily="/Guangzhou.PlatformScreen;component/Assets/digital-7.ttf#Digital-7"/>
                    <TextBlock Text="月" FontSize="24" Margin="8,0,4,0"/>
                    <TextBlock Text="{Binding CurrentTime,Converter={StaticResource DateTimeToDayConverter}}" Margin="4,0,8,0" FontSize="28" Foreground="#FECC00" Background="Black" FontFamily="/Guangzhou.PlatformScreen;component/Assets/digital-7.ttf#Digital-7"/>
                    <TextBlock Text="日" FontSize="24"/>
                </StackPanel>
            </Grid>
        </StackPanel>
        <Grid Grid.Row="0" MinWidth="350">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="←" VerticalAlignment="Center" TextAlignment="Left" FontFamily="Cascadia Code" FontSize="42" Margin="8,0,0,0"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding ScreenA[0].TrainNumber}" VerticalAlignment="Center" TextAlignment="Center" FontSize="42"/>
            <TextBlock Grid.Row="0" Grid.Column="2" Text="次" VerticalAlignment="Center" TextAlignment="Left"/>
            <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding ScreenA[0].DepartureTime,Converter={StaticResource DateTimeToStringConverter}}" VerticalAlignment="Center" TextAlignment="Center"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="开往" VerticalAlignment="Center" TextAlignment="Center"/>
            <TextBlock Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" Text="{Binding ScreenA[0].Terminal}" VerticalAlignment="Center" TextAlignment="Center"/>
            <pp:RunningText Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Speed="20" FontSize="26"
                            Text="{Binding Text}" Foreground="#C3EF66"/>
        </Grid>
    </StackPanel>

</Page>
