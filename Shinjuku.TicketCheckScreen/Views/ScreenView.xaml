<Page x:Class="Shinjuku.TicketCheckScreen.Views.ScreenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" Background="Black"
        xmlns:converters="clr-namespace:CRSim.ScreenSimulator.Converters;assembly=CRSim.ScreenSimulator"
        xmlns:converters1="clr-namespace:Shinjuku.TicketCheckScreen.Converters"
        FontFamily="Courier New" FontSize="38"
        xmlns:views="clr-namespace:CRSim.ScreenSimulator.Views;assembly=CRSim.ScreenSimulator">
    <Page.Resources>
        <converters1:TrainNumberToColorConverter x:Key="TrainNumberToColorConverter"/>
        <converters1:StringConverter x:Key="StringConverter"/>
        <converters:DateTimeToStringConverter x:Key="DateTimeToStringConverter" Format="HH:mm" />
    </Page.Resources>
    <views:BaseDataGrid ItemsSource="{Binding ScreenA}" BorderThickness="0" Margin="3"
                        RowHeight="36" HeadersVisibility="None" FontWeight="Bold">
        <DataGrid.Columns>
            <DataGridTextColumn Binding="{Binding TrainNumber}" MinWidth="130">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="Background" Value="{Binding Path=TrainNumber,Converter={StaticResource TrainNumberToColorConverter}}"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="2,0,0,0"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>
            <DataGridTextColumn Binding="{Binding DepartureTime, Converter={StaticResource DateTimeToStringConverter}}" MinWidth="150">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="TextAlignment" Value="Center"/>
                        <Setter Property="Foreground" Value="White"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>
            <DataGridTextColumn Binding="{Binding Terminal, Converter={StaticResource StringConverter}}" MinWidth="125">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontFamily" Value="等线"/>
                        <Setter Property="FontSize" Value="32"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>
            <DataGridTextColumn Binding="{Binding Platform}">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Foreground" Value="White"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>
        </DataGrid.Columns>
        <DataGrid.RowStyle>
            <Style TargetType="DataGridRow">
                <Setter Property="Background" Value="Black"/>
            </Style>
        </DataGrid.RowStyle>
    </views:BaseDataGrid>
</Page>
